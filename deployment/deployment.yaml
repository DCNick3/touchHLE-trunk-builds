apiVersion: apps/v1
kind: Deployment
metadata:
  name: touchhle-trunk-builds
spec:
  replicas: 1
  selector:
    matchLabels:
      app: touchhle-trunk-builds
  template:
    metadata:
      labels:
        app: touchhle-trunk-builds
      annotations:
        instrumentation.opentelemetry.io/inject-sdk: "true"
    spec:
      containers:
        - name: touchhle-trunk-builds
          image: ghcr.io/dcnick3/touchhle-trunk-builds:ref-1587c53142ed84d60a475e72379e987749f6aac7
          env:
            - name: CONFIG_GITHUB__TOKEN
              valueFrom:
                secretKeyRef:
                  name: touchhle-trunk-builds-secrets
                  key: github_token
